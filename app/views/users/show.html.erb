<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">

  </head>
  <body>
  	<br><br>
  	<div class="container">

      <h1>Vos informations :</h1>
      <br>
      <p class="lead">Si vous souhaitez les modifier <%= link_to "C'est par ici !", edit_user_path, class: "btn btn-primary btn-sm"%></p>
      <br>

      <p>Si vous souhaitez modifier vos paramètres de connexion <%= link_to "C'est par là !", edit_user_registration_path, class: "btn btn-primary btn-sm"%></p>

      <br>

      <div class="row">
        <div class="col-4">Prénom :</div>
        <div class="col-4"><%= current_user.first_name %></div>
      </div>

      <div class="row">
        <div class="col-sm-4">Nom :</div>
        <div class="col-sm-4"><%= current_user.last_name %></div>
      </div>

      <div class="row">
        <div class="col-md-4">Description :</div>
        <div class="col-md-4"><%= current_user.description %></div>
      </div>

      <div class="row">
        <div class="col-lg-4">Email :</div>
        <div class="col-lg-4"><%= current_user.email %></div>
      </div>

      <div class="row">
        <div class="col-xl-4">Evénements que vous avez organisés :</div>
      </div>
      <br>
    </div>
    <div class="card-deck">
	  <% current_user.organised_events.each do |organised_event| %>
	    <div class="col-sm-4 mb-3">
	      <div class="card">
	        <div class="card-body">
	          <h5 class="card-title">Titre : <%= organised_event.title %></h5>
	          <p class="card-text"><%= organised_event.description %></p>
	        </div>
	      </div>
	    </div>
	  <% end %>
	</div>
  </body>
</html>
